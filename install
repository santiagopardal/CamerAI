touch .env
echo ABSOLUTE_PATH="$(pwd)" >> ./.env

pip install -r requirements.txt
mkdir Images
git submodule init
git submodule update

echo "* 3 * * * $(which python3) $(pwd)/src/merger/merge.py" | crontab -

cd CamerAPI
./install